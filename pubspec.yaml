name: link
description: A simple widget for simulating links in Flutter.
  It only require the developers to provide a link to the intended website and a widget to represernt the kink.
version: 1.0.0
# homepage: https://www.example.com
author: Germán Alonso <galonsos@gmail.com>

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.18

dev_dependencies:
  test: ^1.20.1
